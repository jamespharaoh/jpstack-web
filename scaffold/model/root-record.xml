<record
	name="root"
	type="root">

	<fields>

		<assigned-id-field/>
		<code-field/>

		<string-field
			name="notice"
			nullable="yes"/>

		<string-field
			name="fixturesSeed"
			nullable="yes"/>

		<integer-field
			name="installationId"/>

	</fields>

	<priv-types>

		<priv-type
			name="Manage"
			description="Full control of the entire system"
			template="yes"/>

		<priv-type
			name="Alert"
			description="See alert notifications in the status bar"
			template="yes"/>

		<priv-type
			name="Debug"
			description="View debug information for the entire system"
			template="yes"/>

	</priv-types>

</record>
