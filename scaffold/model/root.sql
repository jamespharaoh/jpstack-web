SELECT priv_type_insert (
	'root',
	'alert',
	'See alerts',
	'See alert notifications in the status bar',
	true);

SELECT priv_type_insert (
	'root',
	'manage',
	'Full control',
	'Full control over the entire system',
	true);
