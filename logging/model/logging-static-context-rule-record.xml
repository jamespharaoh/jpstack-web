<record
	name="loggingStaticContextRule"
	type="minor">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<parent-field
			type="loggingStaticContext"
			cacheable="yes"/>

		<identity-enum-field
			name="severity"
			type="loggingEventSeverity"/>

		<!-- settings -->

		<yes-no-field
			name="recursive"/>

		<duration-field
			name="keepForDuration"/>

	</fields>

	<collections>

		<associative-collection
			name="events"
			type="loggingEvent"
			table="logging_static_context_rule_event"
			owned="yes"/>

	</collections>

</record>