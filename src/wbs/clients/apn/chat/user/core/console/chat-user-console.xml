<console-module
	name="chat-user">

	<console-helper-provider
		object-name="chatUser"
		view-priv="chat:supervisor">

		<context-stuff
			name="chatUserName"
			field="code"/>

		<context-stuff
			name="chatId"
			delegate="chat"
			field="id"/>

		<priv-key
			name="chatUser.view"
			delegate="chat"
			priv="supervisor"/>

		<priv-key
			name="chatUser.manage"
			delegate="chat"
			priv="manage"/>

		<run-post-processor
			name="chat"/>

	</console-helper-provider>

	<object-context
		name="chatUser"
		object-name="chatUser">

		<list>

			<section
				name="search"
				label="Search">

				<object-search-page
					name="new"
					tab-label="New"
					search-fields="search"
					results-fields="results"
					priv-key="chat.supervisor"/>

				<context-tab-action-page
					name="old"/>
	
				<context-tab-responder
					name="oldResults"
					tab="chatUser.search.old"/>

			</section>

			<context-tab-responder-page
				name="online"/>

			<object-create-page
				fields="create"
				create-time="created"
				target-responder="chatUserCreateResponder"/>

		</list>

		<object>

			<context-tab-responder-page
				name="summary"/>

			<context-tab-action-page
				name="notes"/>

			<extension-point
				name="chatUser.admin"/>

			<extension-point
				name="chatUser.images"/>

			<context-tab-responder-page
				name="history"/>

			<context-tab-responder-page
				name="help"/>

			<context-action-page
				name="helpForm"/>

			<object-events-page
				priv-key="chat.userAdmin"/>

		</object>

	</object-context>

	<field-set
		name="search"
		class="wbs.clients.apn.chat.user.core.model.ChatUserSearch">

		<enum-field
			name="type"
			helper="chatUserTypeConsoleHelper"
			nullable="yes"/>

		<text-field
			name="code"
			nullable="yes"/>

		<enum-field
			name="gender"
			helper="genderConsoleHelper"
			nullable="yes"/>

		<enum-field
			name="orient"
			helper="orientConsoleHelper"
			nullable="yes"/>

		<text-field
			name="numberLike"
			label="Number"
			nullable="yes"/>

		<!-- TODO include deleted -->

		<text-field
			name="name"
			nullable="yes"/>

		<text-field
			name="info"
			nullable="yes"/>

		<text-field
			name="location"
			nullable="yes"/>

		<yes-no-field
			name="hasPicture"/>

		<yes-no-field
			name="hasVideo"/>

		<yes-no-field
			name="adultVerified"/>

		<enum-field
			name="creditMode"
			helper="chatUserCreditModeConsoleHelper"/>

		<currency-field
			name="creditFailed"
			currency="chat.currency"/>

<!--
Credit failed 	to
Credit no reports 	to
Total spent 	to
First join 	to
Dating mode 	
Online 	
Output 	
Order 	
Max results 	
-->

	</field-set>

	<field-set
		name="results"
		object-name="chatUser">

	</field-set>

	<field-set
		name="create"
		object-name="chatUser">

		<gsm-field
			name="name"
			minimum-length="1"
			maximum-length="16"/>

		<enum-field
			name="gender"
			helper="genderConsoleHelper"/>

		<enum-field
			name="orient"
			helper="orientConsoleHelper"/>

		<gazetteer-field
			name="postcode"
			field="locationPlace"
			gazetteer="chat.gazetteer"/>

		<gsm-field
			name="infoText"
			label="Info"
			minimum-length="1"
			maximum-length="160"/>

		<date-field
			name="dob"
			label="Date of birth"/>

		<object-field
			name="category"
			object-type="chatCategory"
			root-field="chat"
			nullable="yes"/>

	</field-set>

</console-module>
