<model-meta
	name="chatBroadcastNumber"
	type="common">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<parent-field
			type="chatBroadcast"/>

		<identity-reference-field
			type="chatUser"/>

		<!-- state -->

		<enum-field
			name="state"
			type="chatBroadcastNumberState"/>

		<!-- data -->

		<reference-field
			name="addedByUser"
			type="user"
			nullable="yes"/>

		<reference-field
			name="removedByUser"
			type="user"
			nullable="yes"/>

		<reference-field
			type="message"
			nullable="yes"/>

	</fields>

</model-meta>