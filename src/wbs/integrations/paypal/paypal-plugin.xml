<plugin
	name="paypal"
	package="wbs.integrations.paypal">

	<plugin-dependencies>
		<plugin-dependency name="integrations-common"/>
		<plugin-dependency name="scaffold"/>
	</plugin-dependencies>

	<models>
		<enum-type name="paypalPaymentState"/>
		<model name="paypalAccount"/>
		<model name="paypalPayment"/>
	</models>

	<fixtures>
		<fixture name="paypal"/>
	</fixtures>

	<layer name="logic">
		<bean class="logic.PaypalApiImplementation"/>
		<bean class="logic.PaypalLogicImplementation"/>
	</layer>

	<console-modules>
		<console-module name="paypal-account"/>
	</console-modules>

</plugin>
