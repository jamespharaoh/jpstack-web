<console-module
	name="sms-arena-route-out">

	<console-helper-provider
		object-name="smsArenaRouteOut"
		view-priv="root:manage">

		<priv-key
			name="smsArena.manage"
			priv="root:manage"/>

		<priv-key
			name="smsArenaRouteOut.manage"
			priv="root:manage"/>

		<context-stuff
			name="smsArenaRouteOutName"
			field="route.name"/>

	</console-helper-provider>

	<object-context
		name="smsArenaRouteOut"
		object-name="smsArenaRouteOut">

		<list>

			<object-list-page
				fields="list"/>

			<object-create-page
				fields="create"/>

		</list>

		<object>

			<object-summary-page
				fields="summary"/>

			<object-settings-page
				fields="settings"/>

			<object-events-page/>

		</object>

	</object-context>

	<field-set
		name="list"
		object-name="smsArenaRouteOut">

		<parent-field/>

		<object-field
			name="smsArenaConfig"
			label="Config"
			object-type="smsArenaConfig"/>

		<text-field
			name="relayUrl"/>

		<text-field
			name="authKey"/>

		<yes-no-field
			name="cid"
			nullable="yes"/>

		<text-field
			name="pid"
			nullable="yes"/>

		<text-field
			name="sid"
			nullable="yes"/>

		<integer-field
			name="mclass"
			nullable="yes"/>

		<text-field
			name="coding"
			nullable="yes"/>

		<text-field
			name="date"
			nullable="yes"/>

		<text-field
			name="validity"
			nullable="yes"/>

	</field-set>

	<field-set
		name="create"
		object-name="smsArenaRouteOut">

		<parent-field/>

		<object-field
			name="smsArenaConfig"
			object-type="smsArenaConfig"/>

		<text-field
			name="relayUrl"/>

		<text-field
			name="authKey"/>

		<yes-no-field
			name="cid"
			nullable="yes"/>

		<text-field
			name="pid"
			nullable="yes"/>

		<text-field
			name="sid"
			nullable="yes"/>

		<integer-field
			name="mclass"
			nullable="yes"/>

		<text-field
			name="coding"
			nullable="yes"/>

		<text-field
			name="date"
			nullable="yes"/>

		<text-field
			name="validity"
			nullable="yes"/>

	</field-set>

	<field-set
		name="summary"
		object-name="smsArenaRouteOut">

		<id-field/>
		<parent-field/>

		<object-field
			name="smsArenaConfig"
			object-type="smsArenaConfig"/>

		<text-field
			name="relayUrl"/>

		<text-field
			name="authKey"/>

		<yes-no-field
			name="cid"
			nullable="yes"/>

		<text-field
			name="pid"
			nullable="yes"/>

		<text-field
			name="sid"
			nullable="yes"/>

		<integer-field
			name="mclass"
			nullable="yes"/>

		<text-field
			name="coding"
			nullable="yes"/>

		<text-field
			name="date"
			nullable="yes"/>

		<text-field
			name="validity"
			nullable="yes"/>

	</field-set>

	<field-set
		name="settings"
		object-name="smsArenaRouteOut">

		<parent-field/>

		<object-field
			name="smsArenaConfig"
			object-type="smsArenaConfig"/>

		<text-field
			name="relayUrl"/>

		<text-field
			name="authKey"/>

		<yes-no-field
			name="cid"
			nullable="yes"/>

		<text-field
			name="pid"
			nullable="yes"/>

		<text-field
			name="sid"
			nullable="yes"/>

		<integer-field
			name="mclass"
			nullable="yes"/>

		<text-field
			name="coding"
			nullable="yes"/>

		<text-field
			name="date"
			nullable="yes"/>

		<text-field
			name="validity"
			nullable="yes"/>

	</field-set>

</console-module>