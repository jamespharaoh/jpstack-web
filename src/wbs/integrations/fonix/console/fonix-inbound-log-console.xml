<console-module
	name="fonix-inbound-log">

	<console-helper-provider
		object-name="fonixInboundLog"
		view-priv="root:manage">

		<priv-key
			name="fonixInboundLog.manage"
			priv="root:manage"/>

		<context-stuff
			name="fonixInboundLogName"
			field="id"/>

	</console-helper-provider>

	<object-context
		name="fonixInboundLog"
		object-name="fonixInboundLog">

		<list>

			<object-search-page
				search-class="wbs.integrations.fonix.model.FonixInboundLogSearch"
				search-form="search"
				results-form="results"/>

		</list>

		<object>

			<object-summary-page
				form="summary"/>

		</object>

	</object-context>

	<forms>

		<form
			name="search"
			class="wbs.integrations.fonix.model.FonixInboundLogSearch"
			type="search">

			<columns>

				<object-field
					name="routeId"
					label="Route"
					object-type="route"
					nullable="yes"/>

				<interval-field
					name="timestamp"
					nullable="yes"/>

				<text-field
					name="details"
					nullable="yes"/>

				<enum-field
					name="type"
					nullable="yes"/>

				<yes-no-field
					name="success"
					nullable="yes"/>

			</columns>

		</form>

		<form
			name="results"
			object-type="fonix-inbound-log"
			type="read-only">

			<columns>

				<timestamp-field
					name="timestamp"/>

				<object-field
					name="route"
					object-type="route"/>

				<enum-field
					name="type"/>

				<yes-no-field
					name="success"/>

			</columns>
n
		</form>

		<form
			name="summary"
			object-type="fonix-inbound-log"
			type="read-only">

			<columns>

				<id-field/>

				<timestamp-field
					name="timestamp"/>

				<object-field
					name="route"
					object-type="route"/>

				<enum-field
					name="type"/>

				<yes-no-field
					name="success"/>

				<text-area-field
					name="details"/>

			</columns>

		</form>

	</forms>

</console-module>
