<console-module
	name="oxygenate-inbound-log">

	<console-helper-provider
		object-name="oxygenateInboundLog"
		view-priv="root:manage">

		<priv-key
			name="oxygenateInboundLog.manage"
			priv="root:manage"/>

		<context-stuff
			name="oxygenateInboundLogName"
			field="id"/>

	</console-helper-provider>

	<object-context
		name="oxygenateInboundLog"
		object-name="oxygenateInboundLog">

		<list>

			<object-search-page
				search-class="wbs.integrations.oxygenate.model.OxygenateInboundLogSearch"
				search-form="search"
				results-form="results"/>

		</list>

		<object>

			<object-summary-page
				form="summary"/>

		</object>

	</object-context>

	<forms>

		<form
			name="search"
			class="wbs.integrations.oxygenate.model.OxygenateInboundLogSearch"
			type="search">

			<columns>

				<object-field
					name="routeId"
					label="Route"
					object-type="route"
					nullable="yes"/>

				<interval-field
					name="timestamp"
					nullable="yes"/>

				<text-field
					name="details"
					nullable="yes"/>

				<enum-field
					name="type"
					nullable="yes"/>

				<yes-no-field
					name="success"
					nullable="yes"/>

			</columns>

		</form>

		<form
			name="results"
			object-type="oxygenate-inbound-log"
			type="read-only">

			<columns>

				<timestamp-field
					name="timestamp"/>

				<object-field
					name="route"
					object-type="route"/>

				<enum-field
					name="type"/>

				<yes-no-field
					name="success"/>

			</columns>

		</form>

		<form
			name="summary"
			object-type="oxygenate-inbound-log"
			type="read-only">

			<columns>

				<id-field/>

				<timestamp-field
					name="timestamp"/>

				<object-field
					name="route"
					object-type="route"/>

				<enum-field
					name="type"/>

				<yes-no-field
					name="success"/>

				<text-area-field
					name="details"/>

			</columns>

		</form>

	</forms>

</console-module>
