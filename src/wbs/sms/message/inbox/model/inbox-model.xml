<model-meta
	name="inbox"
	type="ephemeral">

	<fields>

		<!-- id -->

		<foreign-id-field
			field="message"/>

		<!-- identity -->

		<master-field
			type="message"/>

		<!-- data -->

		<timestamp-field
			name="createdTime"
			column-type="iso"/>

		<!-- statistics -->

		<integer-field
			name="numAttempts"
			default="0"/>

		<!-- state -->

		<enum-field
			name="state"
			type="inboxState"/>

		<timestamp-field
			name="nextAttempt"
			column-type="iso"
			nullable="yes"/>

		<string-field
			name="statusMessage"
			nullable="yes"/>

	</fields>

	<priv-types>

		<!-- root -->

		<priv-type
			subject="root"
			name="Inbox view"
			description="View SMS messages in the inbox"
			template="yes"/>

		<!-- slice -->

		<priv-type
			subject="slice"
			name="Inbox view"
			description="View SMS messages in the inbox for this slice"
			template="yes"/>

	</priv-types>

</model-meta>