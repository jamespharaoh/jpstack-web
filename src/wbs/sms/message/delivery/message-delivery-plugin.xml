<plugin
	name="delivery"
	package="message.delivery">

	<sql-scripts>
		<sql-schema name="delivery"/>
	</sql-scripts>

	<models>
		<model name="delivery"/>
		<model name="deliveryType"/>
	</models>

	<layer name="daemon">
		<bean class="daemon.DeliveryDaemon"/>
	</layer>

</plugin>
