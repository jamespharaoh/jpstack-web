<plugin
	name="keyword"
	package="wbs.sms.keyword">

	<plugin-dependencies>
		<plugin-dependency name="sms-core"/>
	</plugin-dependencies>

	<sql-scripts>
		<sql-schema name="keyword-set-fallback"/>
		<sql-data name="keyword-data"/>
	</sql-scripts>

	<models>
		<model name="keyword"/>
		<model name="keywordSet"/>
		<model name="keywordSetFallback"/>
	</models>

	<layer name="logic">
		<bean class="logic.KeywordLogicImpl"/>
	</layer>

	<layer name="daemon">
		<bean class="daemon.KeywordCommand"/>
	</layer>

	<fixtures>
		<fixture name="keyword"/>
	</fixtures>

	<console-modules>
		<console-module name="keyword"/>
		<console-module name="keyword-set"/>
	</console-modules>

</plugin>
