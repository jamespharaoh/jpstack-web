<plugin
	name="route-tester"
	package="route.tester">

	<sql-scripts>
		<sql-schema name="route-tester"/>
	</sql-scripts>

	<models>
		<model name="routeTest"/>
		<model name="routeTester"/>
	</models>

	<layer name="daemon">
		<bean class="daemon.RouteTesterCommand"/>
		<bean class="daemon.RouteTesterDaemon"/>
	</layer>

</plugin>
