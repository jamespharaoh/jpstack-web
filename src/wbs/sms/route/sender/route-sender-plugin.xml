<plugin
	name="route-sender"
	package="wbs.sms.route.sender">

	<plugin-dependencies>
		<plugin-dependency name="sms-core"/>
	</plugin-dependencies>

	<models>
		<model name="sender"/>
	</models>

	<fixtures>
		<fixture name="sender"/>
	</fixtures>

	<layer name="entity">
		<bean class="model.SenderBuilder"/>
		<bean class="model.SenderSpec"/>
		<bean class="model.SendersBuilder"/>
		<bean class="model.SendersSpec"/>
	</layer>

	<console-modules>
		<console-module name="sender"/>
	</console-modules>

</plugin>
