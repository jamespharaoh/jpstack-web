<model-meta
	name="chatUserName"
	type="event">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<parent-field
			type="chatUser"/>

		<!-- TODO index -->

		<timestamp-field
			name="creationTime"
			column-type="postgresql"
			column="timestamp"/>

		<!-- data -->

		<string-field
			name="originalName"
			column="name"
			nullable="yes"/>

		<string-field
			name="editedName"/>

		<reference-field
			name="moderator"
			type="user"
			column="user_id"
			nullable="yes"/>

		<enum-field
			name="status"
			type="chatUserInfoStatus"/>

		<timestamp-field
			name="moderationTime"
			column-type="postgresql"
			nullable="yes"/>

		<integer-field
			name="threadId"
			nullable="yes"/>

		<enum-field
			name="editReason"
			type="chatUserEditReason"/>

	</fields>

</model-meta>