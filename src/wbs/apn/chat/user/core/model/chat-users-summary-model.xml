<model-meta
	name="chatUsersSummary"
	type="common"
	create="no">

	<fields>

		<!-- id -->

		<foreign-id-field
			field="chat"/>

		<!-- identity -->

		<master-field
			type="chat"/>

		<!-- statistics -->

		<integer-field
			name="numUsersGayMale"/>

		<integer-field
			name="numUsersGayFemale"/>

		<integer-field
			name="numUsersBiMale"/>

		<integer-field
			name="numUsersBiFemale"/>

		<integer-field
			name="numUsersStraightMale"/>

		<integer-field
			name="numUsersStraightFemale"/>

		<integer-field
			name="numMonitorsGayMale"/>

		<integer-field
			name="numMonitorsGayFemale"/>

		<integer-field
			name="numMonitorsBiMale"/>

		<integer-field
			name="numMonitorsBiFemale"/>

		<integer-field
			name="numMonitorsStraightMale"/>

		<integer-field
			name="numMonitorsStraightFemale"/>

		<integer-field
			name="numJoinedLastDay"/>

		<integer-field
			name="numJoinedLastWeek"/>

		<integer-field
			name="numJoinedLastMonth"/>

		<integer-field
			name="numOnlineLastDay"/>

		<integer-field
			name="numOnlineLastWeek"/>

		<integer-field
			name="numOnlineLastMonth"/>

	</fields>

</model-meta>