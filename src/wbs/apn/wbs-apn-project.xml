<project
	name="wbs-apn"
	package="wbs.apn">

	<plugin
		name="chat-core"
		package="chat.core"/>

	<plugin
		name="chat-ad"
		package="chat.ad"/>

	<plugin
		name="chat-adult"
		package="chat.adult"/>

	<plugin
		name="chat-affiliate"
		package="chat.affiliate"/>

	<plugin
		name="chat-api"
		package="chat.api"/>

	<plugin
		name="chat-approval"
		package="chat.approval"/>

	<plugin
		name="chat-bill"
		package="chat.bill"/>

	<plugin
		name="chat-broadcast"
		package="chat.broadcast"/>

	<plugin
		name="chat-contact"
		package="chat.contact"/>

	<plugin
		name="chat-date"
		package="chat.date"/>

	<plugin
		name="chat-graphs"
		package="chat.graphs"/>

	<plugin
		name="chat-help"
		package="chat.help"/>

	<plugin
		name="chat-user-image"
		package="chat.user.image"/>

	<plugin
		name="chat-user-info"
		package="chat.user.info"/>

	<plugin
		name="chat-info-site"
		package="chat.infosite"/>

	<plugin
		name="chat-user-join"
		package="chat.user.join"/>

	<plugin
		name="chat-keyword"
		package="chat.keyword"/>

	<plugin
		name="chat-named-note"
		package="chat.namednote"/>

	<plugin
		name="chat-scheme"
		package="chat.scheme"/>

	<plugin
		name="chat-settings"
		package="chat.settings"/>

	<plugin
		name="chat-supervisor"
		package="chat.supervisor"/>

	<plugin
		name="chat-user-core"
		package="chat.user.core"/>

	<plugin
		name="chat-user-admin"
		package="chat.user.admin"/>

	<plugin
		name="chat-user-pending"
		package="chat.user.pending"/>

	<plugin
		name="chat-tv-core"
		package="chat.tv.core"/>

	<plugin
		name="chat-tv-moderation"
		package="chat.tv.moderation"/>

</project>
