<console-module
	name="subscription">

	<console-helper-provider
		object-name="subscription">

		<context-stuff
			name="subscriptionName"
			field="name"/>

		<priv-key
			name="subscription.manage"
			priv="manage"/>

		<priv-key
			name="subscription.messages"
			priv="messages"/>

		<priv-key
			name="subscription.stats"
			priv="stats"/>

	</console-helper-provider>

	<object-context
		name="subscription"
		object-name="subscription">

		<list>

			<object-list-page
				fields="list"/>

			<object-create-page/>

		</list>

		<object>

			<object-summary-page
				fields="summary"/>

			<section
				name="settings"
				label="Settings">

				<object-settings-page
					short-name="general"
					fields="settings"/>

				<context-link
					name="keyword"
					link="subscriptionKeyword"
					label="Keywords"
					priv-key="subscription.manage"/>

				<context-link
					name="list"
					link="subscriptionList"
					label="Lists"
					priv-key="subscription.manage"/>

				<context-link
					name="affiliate"
					link="subscriptionAffiliate"
					label="Affiliates"
					priv-key="subscription.affiliate"/>

			</section>

			<section
				name="admin"
				label="Admin">

				<context-link
					name="number"
					link="subscriptionNumber"
					label="Numbers"
					priv-key="subscription.manage"/>

				<context-link
					name="send"
					link="subscriptionSend"
					label="Sends"
					priv-key="subscription.manage"/>

			</section>

			<section
				name="reporting"
				label="Reporting">

				<object-sms-messages-page/>

				<object-sms-stats-page/>

			</section>

			<object-events-page/>

		</object>

	</object-context>

	<field-set
		name="list"
		object-name="subscription">

		<parent-field/>
		<name-field/>
		<description-field/>

	</field-set>

	<field-set
		name="summary"
		object-name="subscription">

		<id-field/>
		<parent-field/>
		<code-field/>
		<name-field/>
		<description-field/>
		<deleted-field/>

		<!-- settings -->

		<object-field
			name="billedRoute"
			object-type="route"/>

		<text-field
			name="billedNumber"/>

		<text-area-field
			name="billedMessage"/>

		<object-field
			name="freeRouter"
			object-type="router"/>

		<text-field
			name="freeNumber"/>

		<integer-field
			name="creditsPerBill"/>

		<integer-field
			name="debitsPerSend"/>

		<text-area-field
			name="subscribeMessageText"/>

		<text-area-field
			name="unsubscribeMessageText"/>

		<!-- statistics -->

		<integer-field
			name="numSubscribers"/>

	</field-set>

	<field-set
		name="settings"
		object-name="subscription">

		<id-field/>
		<code-field/>
		<name-field/>
		<description-field/>
		<deleted-field/>

		<object-field
			name="billedRoute"
			object-type="route"
			nullable="yes"/>

		<text-field
			name="billedNumber"
			nullable="yes"/>

		<text-area-field
			name="billedMessage"
			nullable="yes"/>

		<object-field
			name="freeRouter"
			object-type="router"
			nullable="yes"/>

		<text-field
			name="freeNumber"
			nullable="yes"/>

		<integer-field
			name="creditsPerBill"
			nullable="yes"/>

		<integer-field
			name="debitsPerSend"
			nullable="yes"/>

		<text-area-field
			name="subscribeMessageText"/>

		<text-area-field
			name="unsubscribeMessageText"/>

	</field-set>

</console-module>
