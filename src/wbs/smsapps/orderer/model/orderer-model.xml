<model-meta
	name="orderer"
	type="major">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<parent-field
			type="slice"/>

		<code-field/>

		<!-- details -->

		<name-field/>

		<description-field/>

		<deleted-field/>

		<!-- settings -->

		<reference-field
			name="billRoute"
			type="route"
			nullable="yes"/>

		<string-field
			name="billNumber"
			nullable="yes"/>

		<string-field
			name="billTemplate"
			nullable="yes"/>

		<yes-no-field
			name="allowRepeat"
			nullable="yes"/>

		<string-field
			name="emailAddress"
			nullable="yes"/>

		<string-field
			name="emailSubject"
			nullable="yes"/>

		<string-field
			name="emailTemplate"
			nullable="yes"/>

	</fields>

	<priv-types>

		<priv-type
			name="Manage"
			description="Full control of an orderer service"
			template="yes"/>

		<priv-type
			name="Messages"
			description="View message history for an orderer service"
			template="yes"/>

		<priv-type
			name="Stats"
			description="View message statistics for an orderer service"
			template="yes"/>

		<priv-type
			name="Supervisor"
			description="View supervisor reports for an orderer service"
			template="yes"/>

		<priv-type
			subject="slice"
			name="Orderer create"
			description="Create new orderer service in a slice"
			template="yes"/>

	</priv-types>

	<command-types>

		<command-type
			name="Default"
			description="Place an order with an orderer service"/>

	</command-types>

	<service-types>

		<service-type
			name="Default"
			description="Orderer service"/>

	</service-types>

	<delivery-types>

		<delivery-type
			name="Orderer"
			description="Billed message sent from an orderer service"/>

	</delivery-types>

</model-meta>
