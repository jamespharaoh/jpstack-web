<model-meta
	name="sendSmsConfig"
	type="major">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<parent-field
			type="slice"/>

		<code-field/>

		<!-- details -->

		<name-field/>

		<description-field/>

		<deleted-field/>

		<!-- settings -->

		<reference-field
			name="smsRouter"
			type="router"
			nullable="yes"/>

		<string-field
			name="originator"/>

	</fields>

	<priv-types>

		<priv-type
			name="Manage"
			description="Full control of a send SMS config"
			template="yes"/>

		<priv-type
			name="Messages"
			description="View message history for a send SMS config"
			template="yes"/>

		<priv-type
			name="Stats"
			description="View message stats for a send SMS config"
			template="yes"/>

		<priv-type
			name="Send"
			description="Send a message using a send SMS config"
			template="yes"/>

	</priv-types>

	<service-types>

		<service-type
			name="Default"
			description="Send SMS config"/>

	</service-types>

</model-meta>
