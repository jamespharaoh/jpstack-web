<model-meta
	name="broadcastNumber"
	type="common">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<parent-field
			type="broadcast"/>

		<identity-reference-field
			type="number"/>

		<!-- state -->

		<enum-field
			name="state"
			type="broadcastNumberState"/>

		<!-- data -->

		<reference-field
			name="addedByUser"
			type="user"
			nullable="yes"/>

		<reference-field
			name="removedByUser"
			type="user"
			nullable="yes"/>

		<reference-field
			type="message"
			nullable="yes"/>

	</fields>

</model-meta>