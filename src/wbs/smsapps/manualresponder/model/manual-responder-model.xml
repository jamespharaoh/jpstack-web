<model-meta
	name="manualResponder"
	type="major">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<parent-field
			type="slice"/>

		<code-field/>

		<!-- details -->

		<name-field/>

		<description-field/>

		<deleted-field/>

		<!-- settings -->

		<yes-no-field
			name="canIgnore"
			default="no"/>

		<yes-no-field
			name="canSendMultiple"
			default="no"/>

		<reference-field
			name="infoText"
			type="text"
			nullable="yes"/>

		<yes-no-field
			name="showDailyBillInfo"
			default="no"/>

		<integer-field
			name="preferredQueueTime"
			default="0"/>

		<reference-field
			type="currency"
			nullable="yes"/>

		<reference-field
			name="unblockNumberList"
			type="numberList"
			nullable="yes"/>

		<reference-field
			type="smsCustomerManager"
			nullable="yes"/>

	</fields>

	<collections>

		<children-collection
			name="templates"
			type="manualResponderTemplate"/>

	</collections>

	<priv-types>

		<priv-type
			name="Manage"
			description="Full control of a manual-responder service"
			template="yes"/>

		<priv-type
			name="Messages"
			description="View message history for a manual-responder service"
			template="yes"/>

		<priv-type
			name="Stats"
			description="View message statistics for a manual-responder service"
			template="yes"/>

		<priv-type
			name="Supervisor"
			description="View supervisor reports for a manual-responder service"
			template="yes"/>

		<priv-type
			name="Reply"
			description="Reply to requests received by a manual-responder service"
			template="yes"/>

		<priv-type
			name="Number"
			description="View phone numbers for a manual-responder service"
			template="yes"/>

		<!-- slice -->

		<priv-type
			subject="slice"
			name="Manual responder create"
			description="Create new manual-responder services in a slice"
			template="yes"/>

	</priv-types>

	<command-types>

		<command-type
			name="Default"
			description="Queue a request with a manual-responder service"/>

	</command-types>

	<service-types>

		<service-type
			name="Default"
			description="Manual-responder service"/>

	</service-types>

	<delivery-types>

		<delivery-type
			name="Manual responder"
			description="Messages sent by a manual-responder service"/>

	</delivery-types>

	<queue-types>

		<queue-type
			name="Default"
			description="Pending requests for a manual-responder service"
			subject="number"
			ref="manualResponderRequest"
			preferred-user-delay="preferredQueueTime"
			supervisor-priv="this:supervisor"/>

	</queue-types>

</model-meta>
