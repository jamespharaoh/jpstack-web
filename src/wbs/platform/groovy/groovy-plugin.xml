<plugin
	name="groovy"
	package="wbs.platform.groovy">

	<plugin-dependencies>
		<plugin-dependency name="platform-common"/>
	</plugin-dependencies>

	<layer name="console">
		<bean class="console.GroovyAction"/>
		<bean class="console.GroovyResponder"/>
	</layer>

	<fixtures>
		<fixture name="groovy"/>
	</fixtures>

	<console-modules>
		<console-module name="groovy"/>
	</console-modules>

</plugin>
