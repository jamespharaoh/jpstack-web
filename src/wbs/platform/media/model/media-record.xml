<record
	name="media"
	type="common">

	<fields>

		<!-- id -->

		<generated-id-field/>

		<!-- identity -->

		<!-- TODO -->

		<!-- details -->

		<reference-field
			type="mediaType"/>

		<reference-field
			type="content"/>

		<reference-field
			name="thumbMediaType"
			type="mediaType"
			nullable="yes"/>

		<reference-field
			name="thumb100Content"
			type="content"
			nullable="yes"/>

		<reference-field
			name="thumb32Content"
			type="content"
			nullable="yes"/>

		<string-field
			name="filename"
			nullable="yes"/>

		<string-field
			name="encoding"
			nullable="yes"/>

		<integer-field
			name="width"
			nullable="yes"/>

		<integer-field
			name="height"
			nullable="yes"/>

	</fields>

</record>