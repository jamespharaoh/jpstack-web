<console-module
	name="shn-product-variant">

	<console-helper-provider
		object-name="shnProductVariant"
		view-priv="product.subCategory.category.database:view"
		create-priv="subCategory.category.database:admin">

		<priv-key
			name="shnProductVariant.manage"
			priv="product.subCategory.category.database:admin"/>

		<context-stuff
			name="shnProductVariantName"
			field="itemNumber"/>

		<context-stuff
			name="shnProductId"
			field="product.id"/>

		<run-post-processor
			name="shnProduct"/>

	</console-helper-provider>

	<object-context
		name="shnProductVariant"
		object-name="shnProductVariant">

		<list>

			<object-list-page
				form="list"/>

			<object-create-page
				form="create"/>

		</list>

		<object>

			<object-summary-page
				form="summary"/>

			<object-settings-page
				form="settings"/>

			<object-events-page/>

		</object>

	</object-context>

	<object-context
		name="shnProductVariantSearch"
		object-name="shnProductVariant">

		<list>

			<object-search-page
				search-class="shn.product.model.ShnProductVariantSearch"
				search-form="search"
				results-form="results"/>

		</list>

		<object>

			<object-summary-page
				form="summary"/>

			<object-settings-page
				form="settings"/>

			<object-events-page/>

		</object>

	</object-context>

	<forms>

		<form
			name="list"
			object-type="shn-product-variant"
			type="read-only">

			<columns>

				<code-field pattern="[1-9][0-9]{5}[A-Z]*"/>
				<description-field/>

				<text-field
					name="publicTitle"/>

				<yes-no-field
					name="active"/>

			</columns>

		</form>

		<form
			name="search"
			class="shn.product.model.ShnProductVariantSearch"
			type="search">

			<columns>

				<object-field
					name="productCategoryId"
					object-type="shnProductCategory"
					root-field="product.database"
					nullable="yes"/>

				<object-field
					name="productSubCategoryId"
					object-type="shnProductSubCategory"
					root-field="product.database"
					nullable="yes"/>

				<text-field
					name="itemNumber"
					nullable="yes"/>

				<text-field
					name="description"
					nullable="yes"/>

				<yes-no-field
					name="deleted"
					nullable="yes"
					default="no"/>

				<yes-no-field
					name="active"
					nullable="yes"/>

				<text-field
					name="publicTitle"
					nullable="yes"/>

				<integer-field
					name="stockQuantity"
					nullable="yes"/>

			</columns>

		</form>

		<form
			name="results"
			object-type="shn-product-variant"
			type="read-only">

			<columns>

				<text-field
					name="category"
					field="product.subCategory.category.code"/>

				<text-field
					name="subCategory"
					field="product.subCategory.code"/>

				<code-field/>
				<description-field/>

				<text-field
					name="publicTitle"/>

				<text-field
					name="supplier"
					field="product.supplier.name"/>

				<yes-no-field
					name="active"/>

				<integer-field
					name="stockQuantity"/>

			</columns>

		</form>

		<form
			name="create"
			object-type="shn-product-variant"
			type="create">

			<columns>

				<code-field pattern="[1-9][0-9]{5}[A-Z]*"/>
				<description-field/>

				<text-field
					name="publicTitle"/>

				<yes-no-field
					name="active"/>

			</columns>

		</form>

		<form
			name="summary"
			object-type="shn-product-variant"
			type="read-only"
			fields-provider="shnProductVariantSummaryFieldsProvider">

			<columns>

				<id-field/>
				<parent-field root="product.subCategory.category.database"/>
				<code-field pattern="[1-9][0-9]{5}[A-Z]*"/>
				<description-field/>
				<deleted-field/>

				<text-field
					name="publicTitle"/>

				<yes-no-field
					name="active"/>

				<text-field
					name="supplierReference"/>

				<!-- pricing -->

				<currency-field
					name="recommendedRetailPrice"
					currency="product.subCategory.category.database.currency"/>

				<currency-field
					name="shoppingNationPrice"
					currency="product.subCategory.category.database.currency"/>

				<currency-field
					name="promotionalPrice"
					currency="product.subCategory.category.database.currency"/>

				<currency-field
					name="costPrice"
					currency="product.subCategory.category.database.currency"/>

				<currency-field
					name="postageAndPackaging"
					currency="product.subCategory.category.database.currency"/>

				<!-- inventory -->

				<integer-field
					name="stockQuantity"/>

				<!-- statistics -->

				<integer-field
					name="soldQuantity"/>

				<currency-field
					name="salesRetail"
					currency="product.subCategory.category.database.currency"/>

				<currency-field
					name="salesCost"
					currency="product.subCategory.category.database.currency"/>

			</columns>

		</form>

		<form
			name="settings"
			object-type="shn-product-variant"
			type="update"
			fields-provider="shnProductVariantSettingsFieldsProvider">

			<columns>

				<code-field pattern="[1-9][0-9]{5}[A-Z]*"/>
				<description-field/>
				<deleted-field/>

				<!-- settings -->

				<text-field
					name="publicTitle"
					nullable="yes"/>

				<yes-no-field
					name="active"/>

				<text-field
					name="supplierReference"
					nullable="yes"/>

				<!-- pricing -->

				<currency-field
					name="recommendedRetailPrice"
					currency="product.subCategory.category.database.currency"
					nullable="yes"/>

				<currency-field
					name="shoppingNationPrice"
					currency="product.subCategory.category.database.currency"
					nullable="yes"/>

				<currency-field
					name="promotionalPrice"
					currency="product.subCategory.category.database.currency"
					nullable="yes"/>

				<currency-field
					name="costPrice"
					currency="product.subCategory.category.database.currency"
					nullable="yes"/>

				<currency-field
					name="postageAndPackaging"
					currency="product.subCategory.category.database.currency"
					nullable="yes"/>

				<!-- inventory -->

				<integer-field
					name="stockQuantity"/>

			</columns>

		</form>

	</forms>

</console-module>
